/// Repository
/// Represents a GitHub repository with the details such as name, owner, and ID.
/// Interaction: Used to represent repositories fetched from GitHub.

struct Repository {
    let name: String
    let owner: String
    let repoID: Int
}
